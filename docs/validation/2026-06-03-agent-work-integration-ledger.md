# Agent Work Integration Ledger - 2026-06-03

## Purpose

This ledger records recent agent-produced work as feature units so future work
can consider related changes together without applying the same behavior twice.

The branch was `audit-workpaper-note-reconciliation`, ahead of origin by 25
commits at `c61e669` when the initial ledger was created.

## Current Git Hygiene Findings

- Main worktree was clean at the time this ledger was created.
- A stale prunable worktree registration existed for
  `/private/tmp/dfr-head-compare`; it was removed with `git worktree prune` on
  2026-06-03.
- A zero-byte `.git/objects/maintenance.lock` existed. This was distinct from
  `index.lock` and `HEAD.lock` and did not block status or commits. No active
  `git maintenance` or `git gc` process was observed, so the stale lock was
  removed on 2026-06-03.
- Multiple background tools inspect this repo with Git commands, including
  editor/Codex/Claude status scans and Git fsmonitor. Future commit work should
  check for stale locks before assuming an agent explicitly created them.

## Canonical Feature Units

| Feature key | Status | Canonical commits | Primary files | Notes |
|---|---|---|---|---|
| `local-report-footing-input` | reflected | `3ba7ba0` | `src/dart_footing_reconciler/local_report.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_cli.py`, `tests/test_package.py` | Local DSD/HTML attachment loading, Korean encoding decode, PDF/network rejection, package export. Do not add a second local loader; extend this module. |
| `reconciliation-cashflow-evidence-tightening` | reflected | `f40abd2` | `src/dart_footing_reconciler/checks_reconciliation.py`, `src/dart_footing_reconciler/reconciliation_inputs.py`, `tests/test_checks_reconciliation.py` | Covers implausible balance candidates as `parse_uncertain`, accumulated depreciation disposal adjustments, and combined PPE/ROU CFS acquisition handling. Future cash-flow fixes must check movement roles here first. |
| `reconciliation-expense-allocation-research-exclusion` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/checks_reconciliation.py`, `tests/test_checks_reconciliation.py`, `HANDOFF.md` | Generalizes the development-cost functional allocation exclusion to research-function labels such as `연구비` when excluding the component reconciles asset amortization/depreciation allocation to expense-by-nature totals. |
| `footing-evidence-source` | reflected | `7c3c612` plus current parser-line follow-up | `src/dart_footing_reconciler/html_tables.py`, `src/dart_footing_reconciler/footing.py`, `tests/test_html_tables.py`, `tests/test_footing.py`, `docs/superpowers/plans/2026-06-02-footing-evidence-source.md` | Footing results preserve deterministic table/row/column evidence coordinates in `FootingEvidence.source` and now carry parsed HTML cell line numbers in `FootingEvidence.line`. |
| `full-note-layout-coverage` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/note_inventory.py`, `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/coverage.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/corpus.py` | Scans every note table, classifies known vs unknown layouts, and reports company-level and corpus-level note/table coverage. Future company-format fixes should add layout variants and fixtures here rather than company-name branches. |
| `layout-aware-target-extraction` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/reconciliation_inputs.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_reconciliation_inputs.py` | Detects table orientation, emits unit-normalized source-backed verification candidates, generates trusted roll-forward formula diagnostics, and preserves layout/orientation metadata on extracted note balances and movements. Future validation integration should consume these candidates instead of re-parsing row/column semantics ad hoc. |
| `asset-measure-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes column-oriented asset measurement summary tables with asset-topic rows and gross/accumulated/carrying-amount columns as `asset_measure_summary`, then emits ending carrying-amount candidates. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-asset-measure-summary` kept primary 243/190/53, increased known layout tables from 1,052 to 1,404, and reduced validation-relevant unknown layout items from 9,949 to 9,625. |
| `asset-movement-columns-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes mixed asset movement tables where movement roles are columns and asset classes are rows as `asset_movement_columns`, then extracts source-backed candidates from the asset total row only. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-asset-movement-columns` kept primary 243/190/53, increased known layout tables from 1,404 to 1,708, and reduced validation-relevant unknown layout items from 7,676 to 7,401. |
| `embedded-asset-movement-orientation` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Detects mixed orientation for embedded movement/account headers such as `기초 유형자산` when asset rows and substantive asset-change columns are present, while guarding tax temporary-difference tables. Maps `제거` to disposal candidates. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-embedded-movement-orientation` kept primary 243/190/53 and reduced validation-relevant unknown layout items from 7,401 to 7,385. |
| `right-of-use-asset-row-movement` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes row-oriented lease-note right-of-use asset movement tables as `asset_row_movement_total`, maps lease-specific movements such as `종료` and `리스변경`, and extracts source-backed candidates through the existing row-oriented path. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-rou-row-movement` kept primary 243/190/53, increased known layout tables from 1,708 to 1,773, and reduced validation-relevant unknown layout items from 7,385 to 7,322. |
| `asset-stacked-measure-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes stacked asset measurement summaries where the asset topic is a header and rows repeat `장부금액` plus `장부금액 합계`, then emits the carrying-amount total as an ending candidate with source coordinates and unit multiplier. 엘앤에프 one-company candidate report recorded `asset_stacked_measure_summary: 4` and `verification_candidates: 10`; latest no-fetch 100-company run `out/corpus/run_2026-06-05-asset-stacked-measure-summary` kept primary 243/190/53, increased known layout tables from 1,773 to 1,803, and reduced validation-relevant unknown layout items from 6,987 to 6,943. |
| `financial-instrument-category-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes financial instrument category tables with account rows, financial measurement/category columns, and either a `범주 합계`/financial total column or a final `합계`/`총계` row. Row-wise formulas still verify category components against row totals. Column-total formulas now verify account-row sums against category-column total rows through `financial_category_column_total`, with official `note_layout_formula_check` promotion limited to matched formulas. The financial total-column detector no longer falls back to the last column, preventing category headers such as `기타포괄손익...금융자산` from being treated as totals. INVENI one-company candidate report recorded `financial_instrument_category_summary: 8` and reduced validation-relevant unknown layout items to 57; earlier no-fetch run `out/corpus/run_2026-06-05-financial-category-summary` kept primary 243/190/53, increased known layout tables from 1,803 to 2,207, and reduced balance reconciliation unknown candidates from 4,268 to 4,070. Latest no-fetch run `out/corpus/run_2026-06-05-financial-category-column-totals` added 18 matched note assertions, reduced parse-uncertain by 8, increased known layout tables by 160, and reduced validation-relevant unknown layout items by 64 without increasing unexplained gaps. |
| `receivable-present-value-carrying-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes receivable gross/discount/allowance/carrying-amount tables with headers such as `총장부금액`, `현재가치할인차금`, `손상차손누계액`, `대손충당금`, `손실충당금`, and `이연대출부대수익(비용)`, then verifies row-level component sums to `장부금액 합계` through `receivable_carrying_total` diagnostics. Two-label rows use the specific second label for account keys. Direct 100-company scan found 16 companies / 54 tables / 404 formulas: matched 396, unexplained_gap 8, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-receivable-present-value-carrying-summary` kept primary 243/190/53, increased total checks 65,909 -> 66,313, matched 32,144 -> 32,540, known layout tables 4,058 -> 4,064, validation-relevant unknown layout items 5,113 -> 5,107, and left the 8 삼성E&A unexplained row differences as follow-up diagnostics rather than forced matches. |
| `inventory-carrying-formula-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Extends `inventory_carrying_amount_summary` from ending-only candidates into row-level formula diagnostics for inventory gross/cost, valuation allowance, impairment allowance, and carrying amount tables. Supports total labels such as `유동재고자산`, `총유동재고자산`, and `재고자산 합계`, two-label rows where the second label is the actual inventory class, and gross headers such as `총장부금액`, `취득원가`, and `평가전금액`. Direct 100-company scan found 63 companies / 228 tables / 1,404 formulas: matched 1,378, unexplained_gap 26, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-inventory-carrying-formula-summary` kept primary 243/190/53, increased total checks 66,313 -> 67,717, matched 32,540 -> 33,942, known layout tables 4,064 -> 4,194, validation-relevant unknown layout items 5,107 -> 4,979, and left the +2 corpus gaps as true table arithmetic follow-up diagnostics rather than forced matches. |
| `lease-liability-current-noncurrent-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes lease note current/non-current liability amount summaries where `유동 리스부채` plus `비유동 리스부채` closes to `합계`/`리스부채 합계`/`총 리스부채`. Emits source-backed `lease_liability_split_component` and `ending` candidates, promotes `lease_liability_split_total` formulas to `note_layout_formula_check`, and excludes maturity-bucket tables so `리스부채 만기분석` stays in `lease_liability_maturity_summary`. Direct 100-company scan found 27 companies / 100 tables / 100 formulas, all matched. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-lease-liability-current-noncurrent-summary` kept primary 243/190/53, increased total checks 67,717 -> 67,817, matched 33,942 -> 34,042, known layout tables 4,194 -> 4,294, unknown layout tables 36,648 -> 36,548, and validation-relevant unknown layout items 4,979 -> 4,971 without adding unexplained or parse-uncertain formulas. |
| `financial-fair-value-level-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes financial fair-value hierarchy tables with `수준1`/`수준2`/`수준3` columns and a `합 계` row total as `financial_fair_value_level_summary`. Emits source-backed `fair_value_level_component` and `fair_value_total` candidates per row, uses the second label column for actual instrument names, and promotes row-level `financial_fair_value_level_total` formulas to `note_layout_formula_check`. Direct 100-company scan found 2 companies / 9 tables / 37 formulas, all matched. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-financial-fair-value-level-summary` kept primary 243/190/53, increased total checks 67,817 -> 67,854, matched 34,042 -> 34,079, known layout tables 4,294 -> 4,394, unknown layout tables 36,548 -> 36,448, and validation-relevant unknown layout items 4,971 -> 4,964 without adding unexplained or parse-uncertain formulas. |
| `tax-expense-composition-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes simple income-tax expense composition tables with period columns as `tax_expense_composition_summary`. Emits source-backed `tax_expense_component` candidates for current tax burden/adjustment, deferred-tax movement, capital-direct tax effects, and simple `기타` rows, then verifies period-level `tax_expense_composition_total` formulas against the final `법인세비용`/`법인세비용(수익)` row. Excludes effective-tax reconciliation and OCI tax-effect detail tables. Direct 100-company scan found 10 companies / 19 tables / 37 formulas, all matched. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-tax-expense-composition-summary` kept primary 243/190/53, increased total checks 67,854 -> 67,891, matched 34,079 -> 34,116, known layout tables 4,394 -> 4,413, unknown layout tables 36,448 -> 36,429, and did not add unexplained or parse-uncertain formulas. |
| `credit-risk-exposure-row-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Extends `credit_risk_exposure_summary` to horizontal maximum credit-risk exposure tables where `신용위험에 대한 최대 노출정도` is a row and financial assets/guarantees/loan commitments are columns ending in `금융상품 합계`. Emits source-backed `credit_exposure_component` candidates for every numeric component column before the total, uses generic `credit_risk_exposure_component` when account taxonomy is unclear, and verifies row-level `credit_risk_exposure_total` formulas through `note_layout_formula_check`. Direct 100-company implementation-path scan found 8 companies / 28 tables / 30 formulas, all matched. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-credit-risk-exposure-row-summary` kept primary 243/190/53, increased total checks 67,891 -> 67,921, matched 34,116 -> 34,146, known layout tables 4,413 -> 4,437, unknown layout tables 36,429 -> 36,405, validation-relevant unknown layout items 4,964 -> 4,940, and did not add unexplained or parse-uncertain formulas. |
| `provision-current-noncurrent-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes provision component summary tables where provision account rows close separately to `유동`/`비유동` or `유동충당부채`/`비유동충당부채` column totals such as `기타충당부채 합계`. Keeps this separate from provision roll-forwards, emits source-backed `provision_column_component` and `provision_column_total` candidates, includes provision-account labels such as `기타장기종업원급여부채` even when they omit literal `충당부채`, and promotes per-column `provision_column_total` formulas to `note_layout_formula_check`. Direct 100-company implementation-path scan found 5 companies / 14 tables / 28 formulas, all matched. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-provision-current-noncurrent-summary` kept primary 243/190/53, increased total checks 67,921 -> 67,945, matched 34,146 -> 34,174, known layout tables 4,437 -> 4,447, unknown layout tables 36,405 -> 36,395, validation-relevant unknown layout items 4,940 -> 4,930, and did not add unexplained formulas. |
| `receivable-carrying-amount-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes receivable carrying-amount summaries with `총장부금액`, impairment allowance, and `장부금액 합계` columns, then emits ending candidates for trade receivables, other receivables, loans, accrued income, and deposits while preserving row/column coordinates and units. INVENI one-company candidate report recorded `receivable_carrying_amount_summary: 4` and `verification_candidates: 112`; latest no-fetch 100-company run `out/corpus/run_2026-06-05-receivable-carrying-summary` kept primary 243/190/53, increased known layout tables from 2,207 to 2,383, and reduced balance reconciliation unknown candidates from 4,070 to 3,900. |
| `loss-allowance-rollforward-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `HANDOFF.md` | Recognizes receivable loss-allowance roll-forward tables where account labels appear in a secondary header row and movement rows include `기초 손실충당금`, expected credit loss, reversal, write-off, and ending allowance. Extracts per-account signed movement candidates and supports `signed_rollforward` formulas. INVENI candidate report recorded `loss_allowance_rollforward: 4`, `verification_candidates: 142`, and `matched_formulas: 2`; no-fetch 100-company run `out/corpus/run_2026-06-05-loss-allowance-rollforward` kept primary 243/190/53, increased known layout tables from 2,383 to 2,387, and reduced balance reconciliation unknown candidates from 3,900 to 3,896. |
| `receivable-aging-status-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes receivable aging-status tables with account columns and an `연체상태 합계` row, then emits per-account ending candidates from the total row. INVENI candidate report recorded `receivable_aging_status_summary: 4` and `verification_candidates: 164`; latest no-fetch 100-company run `out/corpus/run_2026-06-05-receivable-aging-summary` kept primary 243/190/53, increased known layout tables from 2,387 to 2,391, and reduced balance reconciliation unknown candidates from 3,896 to 3,892. |
| `inventory-carrying-amount-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes inventory carrying-amount summaries with inventory rows, a `총장부금액`/carrying amount total column, and a total row, then emits the total row as an `inventories` ending candidate with source coordinates and unit multiplier. INVENI candidate report recorded `inventory_carrying_amount_summary: 2`; latest no-fetch 100-company run `out/corpus/run_2026-06-05-inventory-carrying-summary` kept primary 243/190/53, increased known layout tables from 2,391 to 2,465, and reduced balance reconciliation unknown candidates from 3,892 to 3,816. |
| `functional-expense-allocation-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Generalizes functional depreciation/amortization allocation tables even when the note title is an asset note rather than an expense note. Detects `매출원가`, SG&A, and `기능별 항목 합계` columns with depreciation/amortization rows, and emits the total column as an `expense_allocation_total` candidate. INVENI candidate report recorded `functional_expense_allocation: 4`; latest no-fetch 100-company run `out/corpus/run_2026-06-05-functional-expense-allocation` kept primary 243/190/53, increased known layout tables from 2,465 to 2,518, and reduced expense allocation unknown candidates from 869 to 687. |
| `asset-cost-accumulated-component-net-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes asset cost/accumulated-depreciation summary tables without requiring a carrying-amount header as `asset_cost_accumulated_summary`. Emits source-backed gross cost, accumulated depreciation, accumulated impairment, and ending candidates when available, and verifies `gross_cost + accumulated_depreciation + accumulated_impairment = ending` through `component_net` formula diagnostics. Direct 100-company scan found 4 tables: INVENI note 14 tables 68/69 and 아남전자 note 7 tables 108/109. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-asset-component-net-summary` kept primary 243/190/53, increased known layout tables from 2,518 to 2,522, and reduced validation-relevant unknown layout items from 6,309 to 6,305. |
| `debt-instrument-detail-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes borrowing/bond detail tables with repeated `차입금명칭` headers and debt detail rows as `debt_instrument_detail_summary`. Emits source-backed debt total, current portion, ending, face amount, present-value or bond discount candidates, then verifies `debt_total + current_portion = ending`, `face_amount + debt_discount = debt_total`, or direct `face_amount + debt_discount + current_portion = ending` when no intermediate debt total row is present. Title-scoped borrowings now override incidental `사채 제외` text. Formula diagnostics still expose parse-uncertain and non-closing cases, but official `note_layout_formula_check` promotion is matched-only until section/row role coverage is stronger. INVENI candidate report recorded `debt_instrument_detail_summary: 4`, `verification_candidates: 190`, and `matched_formulas: 6`; direct 100-company scan found 268 tables across 40 companies. Earlier no-fetch run `out/corpus/run_2026-06-05-debt-instrument-detail-summary` kept primary 243/190/53, increased known layout tables from 2,522 to 2,790, and reduced validation-relevant unknown layout items from 6,305 to 6,035. Latest no-fetch run `out/corpus/run_2026-06-05-debt-detail-matched-assertions` added 9 matched note assertions with no unexplained or parse-uncertain increase. |
| `debt-component-column-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Extends `debt_instrument_detail_summary` to bond detail tables where the summary row carries component columns such as `명목금액`, `차감: 유동성사채`, `차감: 사채할인발행차금`, and `비유동 사채의 비유동성 부분`. Uses a header-specific role mapper for component-column tables so existing row-label debt split tables keep `비유동성 차입금` as ending rather than current portion. 현대위아 direct scan matched table 104/105, and latest no-fetch run `out/corpus/run_2026-06-05-debt-component-column-summary` added 13 matched note assertions, increased known layout tables by 10, reduced unknown layout tables by 10, and reduced validation-relevant unknown layout items by 8 without adding unexplained or parse-uncertain formulas. |
| `earnings-per-share-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Recognizes EPS tables where profit rows use the table display unit but weighted-average share-count and EPS result rows must remain unscaled. Emits source-backed `eps_profit`, `weighted_average_shares`, and `earnings_per_share` candidates grouped by continuing/discontinued/basic EPS, then verifies rounded `profit / shares = EPS` through `note_layout_formula_check`. INVENI direct scan matched 4 EPS checks, and latest no-fetch run `out/corpus/run_2026-06-05-eps-summary` added 74 matched note assertions, increased known layout tables by 147, reduced unknown layout tables by 147, and did not add unexplained or parse-uncertain formulas. |
| `dividend-payout-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Recognizes period-column dividend summary tables where net income and cash dividends are disclosed in millions of won while payout ratio is disclosed as percent. Emits period-grouped net income, cash dividend, and tenth-percent payout candidates, then verifies `cash_dividends / net_income * 1000 = payout_ratio_tenths` through `note_layout_formula_check`. INVENI direct scan matched 2 payout checks, and latest no-fetch run `out/corpus/run_2026-06-05-dividend-payout-summary` added 127 matched note assertions, increased known layout tables by 99, reduced unknown layout tables by 99, and did not add unexplained or parse-uncertain formulas. |
| `employee-benefit-contribution-maturity-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Extends `employee_benefit_maturity_summary` to expected contribution maturity rows such as `다음 연차보고기간 동안에 납부할 것으로 예상되는 기여금`. Emits source-backed maturity bucket components and total under `defined_benefit_expected_contributions`, while skipping total-only rows with fewer than two maturity component buckets so official checks do not create false `actual=0` gaps. Direct 100-company scan found 현대위아 4 matched checks and 현대비앤지스틸 2 matched checks, unresolved 0. Latest 100-company `--results-only` run `out/corpus/run_2026-06-06-employee-benefit-contribution-maturity-summary` added 6 matched note assertions, increased known layout tables by 15, reduced unknown layout tables by 15, reduced validation-relevant unknown layout items by 12, and did not add unexplained or parse-uncertain formulas. |
| `provision-rollforward-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Recognizes provision movement tables with `기초`, `전입`, usage, scope-change, held-for-sale, and ending columns as `provision_rollforward`. Emits beginning, signed movement, and ending candidates with source coordinates and reuses signed roll-forward formula diagnostics. INVENI candidate report recorded `provision_rollforward: 2`, `verification_candidates: 202`, and `matched_formulas: 8`; direct 100-company scan found 20 tables across 5 companies. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-provision-rollforward` kept primary 243/190/53, increased known layout tables from 2,790 to 2,810, and reduced validation-relevant unknown layout items from 6,035 to 6,015. |
| `selling-admin-expense-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes selling/admin expense single-amount tables with a `금액` column, expense rows, and a total row as `selling_admin_expense_summary`. Emits source-backed expense components and total candidates, then verifies component sum to total through `expense_summary_total` diagnostics. INVENI candidate report recorded `selling_admin_expense_summary: 2`, `verification_candidates: 234`, and `matched_formulas: 10`; direct 100-company scan found 16 tables across 6 companies. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-selling-admin-expense-summary` kept primary 243/190/53, increased known layout tables from 2,810 to 2,826, and reduced validation-relevant unknown layout items from 6,015 to 6,013. |
| `net-debt-bridge-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes cash-flow note debt bridge tables with `기초 순부채`/cash-flow/`기말 순부채` rows and financial liability account columns as `net_debt_bridge`. Emits source-backed beginning, signed movement, and ending candidates per liability column, then groups by account key to generate independent signed roll-forward diagnostics. INVENI source run recorded 4 tables, 71 candidates, and 15 matched formulas; latest 100-company source run `out/corpus/run_2026-06-05-net-debt-bridge` kept primary 243/190/53, increased known layout tables from 2,826 to 2,830, and reduced validation-relevant unknown layout items from 6,013 to 6,009. |
| `financing-debt-bridge-generalization` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_cli.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Generalizes `net_debt_bridge` to cash-flow note tables titled `현금흐름표` whose rows disclose `재무활동에서 생기는 기초/기말 부채` rather than `기초/기말 순부채`. Maps 유동성장기부채, 미지급배당금, and 임대보증금 columns, extracts per-account source-backed signed roll-forward candidates, and promotes formulas into `note_layout_formula_check`. Direct 100-company all-note scan found 29 companies / 89 tables / 2,414 candidates / 355 formulas: matched 310, unexplained_gap 19, parse_uncertain 26. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-financing-debt-bridge` kept primary 243/190/53 while increasing total checks 64,201 -> 64,557, matched 30,574 -> 30,884, known layout tables 3,536 -> 3,621, validation-relevant unknown layout items 5,528 -> 5,443, and validated tables 11,747 -> 11,796. |
| `lease-liability-column-disambiguation` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Splits financing debt bridge lease liability columns by current/noncurrent labels so `유동성리스부채` maps to `current_lease_liabilities` and `(비유동)` lease columns map to `noncurrent_lease_liabilities` instead of being merged into one `lease_liabilities` formula group. Direct 100-company all-note scan over the same 29 companies / 89 net debt bridge tables improved formula statuses from matched 310 / unexplained_gap 19 / parse_uncertain 26 to matched 341 / unexplained_gap 20 / parse_uncertain 10. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-lease-liability-column-disambiguation` kept primary 243/190/53 while increasing matched 30,884 -> 30,915 and reducing parse_uncertain 28,923 -> 28,907. |
| `bond-column-disambiguation` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Splits financing debt bridge bond columns by short-term, current long-term, current, noncurrent, convertible, exchangeable, and combined convertible/exchangeable labels instead of merging them into one `bonds` formula group. Direct 100-company all-note scan over the same 29 companies / 89 net debt bridge tables improved formula statuses from matched 341 / unexplained_gap 20 / parse_uncertain 10 to matched 356 / unexplained_gap 20 / parse_uncertain 1. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-bond-column-disambiguation` kept primary 243/190/53 while increasing matched 30,915 -> 30,930 and reducing parse_uncertain 28,907 -> 28,904. |
| `rental-deposit-column-disambiguation` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Splits financing debt bridge rental deposit columns by current/noncurrent labels so `유동 임대보증금` maps to `current_rental_deposits` and `비유동 임대보증금` maps to `noncurrent_rental_deposits` instead of being merged into one `rental_deposits` formula group. Direct 100-company all-note scan over the same 29 companies / 89 net debt bridge tables improved formula statuses from matched 356 / unexplained_gap 20 / parse_uncertain 1 to matched 358 / unexplained_gap 20 / parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-rental-deposit-column-disambiguation` kept primary 243/190/53 while increasing matched 30,930 -> 30,932 and reducing parse_uncertain 28,904 -> 28,903. |
| `net-debt-aggregate-movement-skip` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Avoids double counting in two-label-column financing debt bridge tables by skipping aggregate signed movement rows whose primary and secondary labels are identical when more specific secondary rows with the same primary label follow. Direct 100-company all-note scan over the same 29 companies / 89 net debt bridge tables reduced candidates 2,414 -> 2,390 and improved formula statuses from matched 358 / unexplained_gap 20 / parse_uncertain 0 to matched 368 / unexplained_gap 10 / parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-net-debt-aggregate-movement-skip` kept primary 243/190/53 while increasing matched 30,932 -> 30,942 and reducing unexplained_gap 4,050 -> 4,040. |
| `display-unit-tolerance` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/formula_discovery.py`, `tests/test_formula_discovery.py`, `HANDOFF.md` | Uses the larger of explicit tolerance and source candidate `unit_multiplier` for roll-forward formula matching, so one displayed unit of rounding difference closes while larger differences remain `unexplained_gap`. Direct 100-company all-note scan over the same 29 companies / 89 net debt bridge tables improved formula statuses from matched 368 / unexplained_gap 10 / parse_uncertain 0 to matched 375 / unexplained_gap 3 / parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-display-unit-tolerance` kept primary 243/190/53 while increasing matched 30,942 -> 30,956 and reducing unexplained_gap 4,040 -> 4,026. |
| `inventory-carrying-allowance-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `HANDOFF.md` | Extends `inventory_carrying_amount_summary` to inventory gross/valuation-allowance/carrying-amount tables whose total row is labeled `재고자산` instead of `합계`. Emits only the total-row `inventories` ending candidate, preserving source coordinates and display unit. 한솔케미칼 `candidate-report` now records `inventory_carrying_amount_summary: 4`; direct 100-company scan shows current inventory carrying summaries across 30 companies / 106 tables / 132 candidates, with the new variant reducing unknown tables in 9 companies / 32 tables. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-inventory-carrying-allowance-summary` kept primary 243/190/53 and total checks/matched unchanged while increasing known layout tables 3,621 -> 3,653 and reducing validation-relevant unknown layout items 5,443 -> 5,411. |
| `loss-allowance-financial-asset-rows` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Extends `loss_allowance_rollforward` to receivable allowance tables whose movement rows are labeled `기초금융자산`/`기말금융자산` and whose account/measure headers are stacked. Requires a `손상차손누계액` measure row before emitting loss-allowance candidates, prioritizes row-oriented loss-allowance movement evidence over repeated `금융상품` headers, and promotes signed roll-forward formulas into `note_layout_formula_check` with effective display-unit tolerance. Direct 100-company scan found 37 companies / 127 tables / 639 candidates / 126 formulas: matched 67, unexplained_gap 47, parse_uncertain 12. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-loss-allowance-financial-asset-rows` kept primary 243/190/53 while increasing total checks 64,586 -> 64,716, matched 30,956 -> 31,023, known layout tables 3,653 -> 3,776, validation-relevant unknown layout items 5,411 -> 5,308, and validated tables 11,796 -> 11,888. |
| `financial-fair-value-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Recognizes financial-instrument fair-value summaries with a single `공정가치` amount column and numeric `금융자산`/`금융부채` total rows as `financial_instrument_fair_value_summary`. Emits source-backed fair-value components and totals, then promotes `financial_fair_value_total` formulas into `note_layout_formula_check`. Direct 100-company scan found 10 companies / 40 tables / 233 candidates / 16 formulas: matched 6, unexplained_gap 10, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-financial-fair-value-summary` kept primary 243/190/53 while increasing total checks 64,716 -> 64,752, matched 31,023 -> 31,029, known layout tables 3,776 -> 3,816, validation-relevant unknown layout items 5,308 -> 5,296, and validated tables 11,888 -> 11,905. |
| `functional-expense-research-allocation` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Recognizes research-and-development functional allocation rows with `매출원가`, `판매비와 일반관리비`, and `기능별 항목 합계` columns as `functional_expense_research_allocation`. Emits source-backed expense components and total only for this layout, then promotes `expense_summary_total` formulas into `note_layout_formula_check` without mixing with depreciation/amortization primary expense-allocation checks. Direct 100-company scan found 4 companies / 9 tables / 22 candidates / 8 formulas: matched 4, unexplained_gap 4, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-functional-expense-research-allocation` kept primary 243/190/53 while increasing total checks 64,752 -> 64,761, matched 31,029 -> 31,033, known layout tables 3,816 -> 3,825, and validation-relevant unknown layout items 5,296 -> 5,287. |
| `employee-benefit-maturity-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Recognizes defined-benefit expected-payment maturity tables as `employee_benefit_maturity_summary` without mixing them with financial-liability liquidity maturity analysis. Emits source-backed maturity bucket components and total for `defined_benefit_expected_payments`, supports bucket headers such as `1~2년 미만` and `5년 이상`, and promotes row formulas into `note_layout_formula_check` with display-unit tolerance. Direct 100-company scan found 22 companies / 53 tables / 255 candidates / 51 formulas: matched 51, unexplained_gap 0, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-employee-benefit-maturity-summary` kept primary 243/190/53 while increasing total checks 64,761 -> 64,812, matched 31,033 -> 31,084, note assertion matched 1,534 -> 1,585, known layout tables 3,825 -> 3,889, and validation-relevant unknown layout items 5,287 -> 5,253. |
| `lease-liability-maturity-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Recognizes lease liability maturity tables as `lease_liability_maturity_summary`, emits row-level maturity bucket components/totals for total lease liabilities, minimum lease payments, lease interest, present value, and lease liability rows, and promotes source-backed formulas into `note_layout_formula_check`. Excludes lessor/receivable lease tables and total-only current/noncurrent rows with fewer than two maturity components. Direct 100-company scan found 32 companies / 102 tables / 587 candidates / 141 formulas: matched 140, unexplained_gap 1, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-lease-liability-maturity-summary` kept primary 243/190/53 while increasing total checks 64,812 -> 64,953, matched 31,084 -> 31,224, known layout tables 3,889 -> 3,959, validated tables 11,905 -> 11,913, and validation-relevant unknown layout items 5,253 -> 5,196. |
| `financial-category-formula-summary` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_layout_formula_assertions.py`, `tests/test_package.py`, `HANDOFF.md` | Extends `financial_instrument_category_summary` to emit source-backed category component candidates and row-level `financial_category_total` formulas for financial-instrument category tables. Supports plain `합계` / `합 계` total headers, `기타금융자산` / `기타금융부채` / `위험회피목적파생상품` component headers, and two-label-column tables where the second label cell is the real account. Direct 100-company scan found 69 companies / 503 tables / 2,342 candidates / 956 formulas: matched 920, unexplained_gap 36, parse_uncertain 0. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-financial-category-formula-summary` kept primary 243/190/53 while increasing total checks 64,953 -> 65,909, matched 31,224 -> 32,144, known layout tables 3,959 -> 4,058, validated tables 11,913 -> 11,968, and validation-relevant unknown layout items 5,196 -> 5,113; the 36 added gaps remain row-level follow-up diagnostics rather than forced matches. |
| `credit-risk-exposure-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes financial-risk note credit exposure summaries with a standalone `신용위험` amount column, financial-asset rows, and a total row as `credit_risk_exposure_summary`. Emits source-backed credit exposure components and total candidates, then verifies component sum to total through `credit_risk_exposure_total` diagnostics. INVENI source run recorded 4 matched formulas before tightening; final guarded 100-company source run `out/corpus/run_2026-06-05-credit-risk-exposure-summary-v2` kept primary 243/190/53, increased known layout tables from 2,830 to 2,832, and reduced validation-relevant unknown layout items from 6,009 to 6,008. |
| `liquidity-maturity-analysis-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes liquidity-risk maturity analysis tables with maturity bucket columns, a total column, and financial-liability rows as `liquidity_maturity_analysis`. Emits source-backed maturity bucket components and row totals, then verifies each row independently through `liquidity_maturity_total` diagnostics grouped by table source, row index, and account key. Latest 100-company source run `out/corpus/run_2026-06-05-liquidity-maturity-analysis` kept primary 243/190/53, increased known layout tables from 2,832 to 3,072, reduced validation-relevant unknown layout items from 6,008 to 5,846, and generated 1,158 row formulas across 62 companies / 238 tables: 887 matched, 271 unexplained_gap, 0 parse_uncertain. |
| `lease-expense-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes lease expense disclosure tables with stacked right-of-use asset headers and a lease asset total column as `lease_expense_summary`. Emits source-backed component/total candidates for right-of-use asset depreciation rows and total-only candidates for lease interest, short-term lease, and low-value asset lease expenses; generates `lease_expense_total` formulas only where components exist. Latest 100-company source run `out/corpus/run_2026-06-05-lease-expense-summary` kept primary 243/190/53, increased known layout tables from 3,072 to 3,103, reduced validation-relevant unknown layout items from 5,846 to 5,815, and generated 29 matched formulas across 9 companies / 31 tables. |
| `discontinued-operation-income-and-cashflow-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes discontinued-operation income statement and cash-flow summary tables as `discontinued_operation_income_statement` and `discontinued_operation_cashflow_summary`. Emits source-backed income roles using both row label cells to disambiguate duplicated `중단영업순이익` rows, verifies six income formulas, and verifies operating/investing/financing cash flow sum to total. INVENI source run recorded 4 tables, 40 candidates, and 14 matched formulas. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-discontinued-operation-income-and-cashflow` kept primary 243/190/53, increased known layout tables from 3,103 to 3,107, reduced validation-relevant unknown layout items from 5,815 to 5,811, and reduced cashflow bridge unknown candidates from 548 to 546. |
| `operating-expense-summary-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_verification_candidates.py`, `tests/test_cli.py`, `HANDOFF.md` | Recognizes operating expense single-amount tables with mixed cost-of-sales and SG&A rows as `operating_expense_summary`, emits `operating_expenses` component/total candidates, and reuses `expense_summary_total` formula diagnostics. INVENI source run recorded 2 tables, 38 candidates, and 2 matched formulas. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-operating-expense-summary` kept primary 243/190/53, increased known layout tables from 3,107 to 3,109, reduced validation-relevant unknown layout items from 5,811 to 5,809, and reduced expense allocation unknown candidates from 669 to 667. |
| `functional-expense-single-row-allocation-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_cli.py`, `HANDOFF.md` | Recognizes asset-note single-row functional allocation tables as `functional_expense_single_row_allocation`, emitting source-backed `expense_allocation_total` candidates without inventing formulas when no internal component sum exists. Direct 100-company scan found 15 companies / 91 tables / 316 candidates. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-functional-expense-single-row-allocation` kept primary 243/190/53, increased known layout tables from 3,109 to 3,200, reduced validation-relevant unknown layout items from 5,809 to 5,718, and reduced expense allocation unknown candidates from 667 to 576. |
| `defined-benefit-rollforward-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/formula_discovery.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/__init__.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_formula_discovery.py`, `tests/test_cli.py`, `tests/test_package.py`, `HANDOFF.md` | Recognizes defined-benefit obligation and plan-asset roll-forward tables as `defined_benefit_rollforward`, emits source-backed beginning/signed movement/ending candidates per account column, skips duplicate total remeasurement rows when detailed remeasurement rows exist, and generates account-level signed roll-forward diagnostics. Direct 100-company scan found 71 companies / 180 tables / 3,411 candidates / 308 formulas: 147 matched, 153 unexplained_gap, 8 parse_uncertain, 0 unknown-account formulas. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-defined-benefit-rollforward` kept primary 243/190/53, increased known layout tables from 3,200 to 3,380, reduced validation-relevant unknown layout items from 5,718 to 5,650, and reduced balance reconciliation unknown candidates from 3,407 to 3,339. |
| `inventory-allowance-rollforward-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `src/dart_footing_reconciler/corpus.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_layout_formula_assertions.py`, `tests/test_cli.py`, `HANDOFF.md` | Recognizes inventory valuation allowance movement tables as `inventory_allowance_rollforward`, maps the `재고자산 평가충당금` amount column to `inventory_valuation_allowance`, and emits source-backed beginning/signed movement/ending candidates. Direct 100-company scan found 한솔케미칼 1 company / 2 tables / 12 candidates / 2 matched signed roll-forward formulas. The formulas are now promoted into workpaper checks as `note_layout_formula_check`, so latest no-fetch 100-company run `out/corpus/run_2026-06-05-inventory-allowance-rollforward` kept primary 243/190/53 while increasing total checks 63,540 -> 63,542, matched 30,273 -> 30,275, known layout tables 3,380 -> 3,382, validation-relevant unknown layout items 5,650 -> 5,648, and validated tables 11,572 -> 11,574. |
| `provision-row-rollforward-layout` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/orientation.py`, `src/dart_footing_reconciler/verification_candidates.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_layout_variants.py`, `tests/test_orientation.py`, `tests/test_verification_candidates.py`, `tests/test_layout_formula_assertions.py`, `tests/test_cli.py`, `HANDOFF.md` | Extends `provision_rollforward` to row-oriented provision movement tables where movement labels are rows and provision account labels are columns. Emits source-backed candidates per provision account column, including headers such as `기타장기종업원급여부채` that omit the literal `충당부채`, groups formulas by account key, and promotes source-backed formulas into `note_layout_formula_check`. Direct 100-company scan found 24 companies / 82 tables / 1,938 candidates with formula statuses matched 81, unexplained_gap 37, parse_uncertain 74. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-provision-row-rollforward` kept primary 243/190/53 while increasing total checks 63,542 -> 63,741, matched 30,275 -> 30,356, known layout tables 3,382 -> 3,450, validation-relevant unknown layout items 5,648 -> 5,580, and validated tables 11,574 -> 11,600. |
| `defined-benefit-title-generalization` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/layout_variants.py`, `src/dart_footing_reconciler/layout_formula_assertions.py`, `tests/test_layout_variants.py`, `tests/test_cli.py`, `tests/test_layout_formula_assertions.py`, `HANDOFF.md` | Generalizes `defined_benefit_rollforward` beyond titles that literally contain `확정급여`/`순확정급여` so employee-benefit notes titled `퇴직급여제도` or `종업원급여` classify when they have defined-benefit obligation and plan-asset account columns plus beginning/ending movement evidence. Promotes defined-benefit signed roll-forward formulas into `note_layout_formula_check`. Direct 100-company scan found 62 companies / 230 tables / 5,086 candidates with formula statuses matched 218, unexplained_gap 234, parse_uncertain 8. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-defined-benefit-title-generalization` kept primary 243/190/53 while increasing total checks 63,741 -> 64,201, matched 30,356 -> 30,574, known layout tables 3,450 -> 3,536, validation-relevant unknown layout items 5,580 -> 5,528, and validated tables 11,600 -> 11,747. |
| `disclosure-noise-relevance-trim` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/validation_relevance.py`, `tests/test_validation_relevance.py`, `HANDOFF.md` | Keeps related-party disclosures, accounting-policy useful-life/amortization tables, and investment-property restriction/remittance disclosures out of formula-layout priority buckets even when they contain asset words. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-relevance-disclosure-trim` kept primary 243/190/53 and reduced validation-relevant unknown layout items from 7,322 to 6,987. |
| `unknown-layout-relevance-taxonomy` | current follow-up | current uncommitted slice | `src/dart_footing_reconciler/validation_relevance.py`, `src/dart_footing_reconciler/corpus.py`, `src/dart_footing_reconciler/cli.py`, `tests/test_validation_relevance.py`, `tests/test_corpus.py`, `tests/test_cli.py`, `HANDOFF.md` | Splits unknown/low-confidence note table backlog into validation-relevant candidates versus general note tables through a reusable classifier shared by corpus output and `candidate-report`. Latest no-fetch 100-company run `out/corpus/run_2026-06-05-relevance-noise-trim` generated 100/100 reports, primary 243/190/53, 39,611 unknown/low-confidence layout items, and 7,676 validation-relevant unknown layout candidates after moving functional depreciation/amortization tables to expense allocation and disclosure-only asset commitment/collateral tables to non-validation. |
| `leadsheet-ui-advisory` | reflected | `fc327ea`, `f5a66b6` | `src/dart_footing_reconciler/report_html.py`, `tests/test_cli_workpaper.py`, `docs/adr/0002-claude-direct-html-ui-implementation.md` | Note-number hover triggers and validation advisory are already implemented. ADR records that direct UI implementation was a one-time exception. |
| `243-baseline-remap` | reflected | `7e72416`, `344d796`, `3408fd8`, `4713478`, `02bd868` | `docs/work-orders/2026-05-31-codex-handoff-reconciliation-logic.md`, `docs/validation/2026-06-01-ab-triage-243-baseline.md`, `HANDOFF.md` | The accepted reproducible local baseline is 243 primary checks / 190 matched / 53 unresolved. The historical 575 baseline is documentation context, not the current acceptance baseline. |
| `audit-worksheet-reporting-core` | reflected, broad | `796f8d5` | many modules under `src/dart_footing_reconciler/`, many tests, report/work-order docs | Large integration commit that introduced reviewer-facing workpaper corpus/reporting and many reconciliation modules. Treat as a broad baseline, not as a model for future mixed-scope commits. |
| `superpowers-artifact-ignore` | reflected | `c61e669` | `.gitignore` | `.superpowers/` is ignored as local agent workspace state. Do not commit generated brainstorm/session state. |
| `election-workpaper-split` | reverted / do not merge here | `d5760b1` through `15e03d1`, reverted by `c8ed18d` | removed `src/election_workpaper/`, removed related tests/docs | This domain was intentionally removed from the DART reconciler repo. Do not reintroduce election workpaper files into this project. |

## Duplicate-Risk Map

| Area | Risk | Guardrail |
|---|---|---|
| Local report input | Another agent may add a separate CLI-only HTML/DSD loader. | Route through `load_local_report()` and `foot_local_report()` in `local_report.py`. |
| Footing source locations | Parser line/DOM source work could duplicate footing coordinate evidence. | Keep `FootingEvidence.source` as local table coordinates; use `FootingEvidence.line` for parsed HTML line evidence and bridge future DOM locations through separate fields rather than replacing coordinates. |
| Cash-flow movement roles | Different agents may add new role names for the same adjustment. | Before adding roles, search `checks_reconciliation.py`, `reconciliation_inputs.py`, and tests for equivalent semantics. |
| Expense allocation exclusions | Different agents may add one-off exclusions for R&D labels. | Extend `_is_research_or_development_function()` and keep the exclusion conditional on reconciling to the nature total. |
| Full-note layout coverage | Company-specific schemas may tempt company-name branches or silent filtering of unknown note tables. | Add layout variants and fixtures through `layout_variants.py`; keep unknown tables visible in `coverage-report`. |
| Layout-aware target extraction | Formula code can accidentally rely on label substrings without preserving table orientation, units, and source coordinates. | Route new formula logic through `VerificationCandidate` and reject unknown or low-confidence layout/orientation evidence for matched formulas. |
| Asset measure summary layout | Column-oriented measurement summaries can be confused with full roll-forward tables or generic total rows. | Keep `asset_measure_summary` limited to asset-topic title/rows plus gross/accumulated/carrying-amount headers, and emit only ending carrying-amount candidates unless explicit movement roles are present. |
| Asset movement columns layout | Movement-column tables can duplicate asset class rows or be mistaken for row-oriented roll-forwards. | Keep `asset_movement_columns` extraction limited to `mixed` orientation and asset total rows; do not emit candidates for each individual asset class row unless a later check needs class-level reconciliation. |
| Asset period roll-forward summary | Asset notes can disclose movement columns with period rows (`당기`/`전기`) rather than asset-class rows, and `-` movement cells should not be coerced into source-backed zero amounts. | Keep `asset_period_rollforward_summary` separate from asset-class movement tables; group candidates by period row, map non-beginning/non-ending headers to `signed_movement`, skip non-parsed `-` cells, and promote only matched source-backed formulas into official note layout checks. |
| Asset two-label row roll-forward summary | Asset roll-forward notes can put a repeated disclosure descriptor in the first label column, true movement labels in the second label column, and one asset-account amount column. Applying asset-style add/subtract semantics to displayed negative movement amounts would invert signs. | Keep `asset_two_label_row_rollforward_summary` limited to asset titles plus asset-account amount columns and repeated `변동/조정` descriptor rows; read movement labels from the second label column, preserve all non-beginning/non-ending rows as `signed_movement`, and promote only matched source-backed signed roll-forward formulas. |
| Embedded asset movement orientation | Headers with asset names embedded in movement labels can overmatch legal/tax schedules. | Require at least three movement headers, asset rows, and at least one substantive asset-change header; keep tax `기초/증감/기말` schedules out of mixed asset orientation. |
| Right-of-use asset row movement | Lease-note movement rows can include lease-specific terms whose sign/direction differs by disclosure. | Keep `asset_row_movement_total` tied to asset-topic rows plus asset-total headers; map lease-specific terms as candidate roles but leave matched-result trust to formula diagnostics and later reconciliation gating. |
| Asset stacked measure summary layout | Header-based asset measurement summaries can be mistaken for generic disclosure totals or movement tables. | Require asset-topic headers plus repeated carrying-amount row labels and extract only the `장부금액 합계` ending candidate unless explicit movement columns are present. |
| Financial instrument category summary layout | Financial category headers often contain `당기손익`, which can be misread as period evidence, and some tables disclose only category-column totals in a final `합계` row rather than a total column. | Detect financial category/account grids before generic period orientation; use only explicit total/category-sum columns for row-wise ending candidates, never a last-column fallback; for total-row-only tables, group by category column and promote only matched `financial_category_column_total` formulas into official note checks. |
| Receivable carrying amount summary layout | Receivable gross/allowance/net tables can be confused with generic asset measurement summaries. | Require receivable title/rows plus gross, allowance, and carrying-amount total columns; map only recognized receivable account rows into candidates. |
| Loss allowance roll-forward layout | Receivable allowance movement tables use signed values rather than asset-style add/subtract semantics and often place account labels on a secondary header row. | Extract all account columns from the secondary header row, preserve signed movement amounts, and use `signed_rollforward` rather than asset roll-forward arithmetic. |
| Receivable aging status summary layout | Aging buckets are disclosure detail and only the total row should feed balance candidates. | Require `연체상태 합계` and recognized receivable account columns; emit only total-row ending candidates. |
| Inventory carrying amount summary layout | Inventory tables may have only a gross/carrying amount column, so generic asset summary rules do not apply. | Require inventory title/rows plus a total row; emit only the total-row `inventories` ending candidate. |
| Functional expense allocation layout | Asset-note depreciation/amortization allocation tables may lack `비용` in the title. | Detect functional cost/SG&A/total columns plus depreciation/amortization rows; emit only the functional total candidate and leave note-to-note reconciliation gating to existing expense allocation checks. |
| Asset cost accumulated component-net layout | Asset gross/accumulated summaries may omit an explicit carrying-amount label or may use `기말` instead, so they can be missed by generic asset measure summary rules. | Keep this layout tied to asset topic plus gross/accumulated headers and a total row; emit component roles with source coordinates, and only mark `component_net` matched when an explicit ending column is present and the arithmetic closes within tolerance. |
| Asset component-column summary | Intangible/PPE notes can disclose component columns such as `상각자산` and `개발 중인 무형자산` plus `장부금액`, which look like asset carrying-amount evidence but are actually row-wise internal table formulas. | Classify these as `asset_component_column_summary` before generic asset carrying totals; extract the most specific row label across multi-label columns, preserve unit multipliers and source coordinates, and verify `asset_component` columns against `asset_component_total` without feeding the table as primary balance evidence. |
| Debt instrument detail summary layout | Borrowing and bond tables can spread total headers across secondary header rows while the inventory only sees the first row, and some labels contain incidental `사채 제외` text inside borrowing tables. | Require debt title, repeated debt-instrument headers, and debt detail rows; extract only the detected total column, keep current portions signed, treat explicit borrowing titles as borrowings despite `사채 제외`, and promote only matched debt detail formulas to official `note_layout_formula_check` until section/row role coverage is strong enough to avoid false gaps. |
| Debt component-column summary layout | Bond detail tables may express the same debt arithmetic as component columns on a summary row, while older debt split tables express roles as row labels; using one role mapper for both can misclassify `비유동성 차입금` as a current portion. | Keep header-role and row-label-role mapping separate, require debt title plus explicit component headers before using the component-column path, and preserve source-backed row/column coordinates for each matched formula. |
| Earnings per share summary layout | EPS tables mix monetary profit rows, share-count rows, and per-share won rows in a single table, so applying the table display unit to every row creates false arithmetic. | Apply table unit multipliers only to profit rows, keep weighted-average shares and EPS results unscaled, and use a dedicated rounded division formula rather than component-sum tolerance. |
| Dividend payout summary layout | Dividend tables mix monetary rows and percent rows, and `parse_amount("58.9")` intentionally returns `589` because the parser is integer-only. | Treat payout ratios as tenth-percent integers, compare `cash_dividends / net_income * 1000`, and keep this formula separate from dividend payable roll-forward or equity/cash-flow reconciliation checks. |
| Provision roll-forward layout | Provision movement tables use signed disclosure amounts, not asset-style add/subtract semantics. | Emit non-beginning/non-ending movement columns as `signed_movement` and let signed roll-forward diagnostics decide matched status; do not reuse asset depreciation/disposal role arithmetic for provisions. |
| Provision row roll-forward layout | Provision schedules can transpose movement labels into rows and provision accounts into columns, with some account headers omitting the literal `충당부채`. | Detect provision account columns from provision-context labels, group candidates by account key, and keep matched/unexplained/parse-uncertain formulas separate in `note_layout_formula_check`. |
| Defined-benefit title generalization | Employee-benefit notes may title the section `퇴직급여제도` or `종업원급여` even when the table is the same defined-benefit roll-forward. | Classify by account headers and movement evidence, not title alone, and surface matched/unexplained/parse-uncertain formulas through `note_layout_formula_check`. |
| Inventory allowance roll-forward layout | Inventory allowance movements use signed allowance balances, not positive inventory carrying amounts. | Map the allowance column to `inventory_valuation_allowance`, preserve displayed signs, route through signed roll-forward diagnostics, and promote only source-backed formulas into `note_layout_formula_check`. |
| Inventory carrying allowance summary | Inventory carrying-amount summary tables can label the total row as `재고자산` while also showing valuation allowance columns. | Treat exact `재고자산` as the inventory total row only inside `inventory_carrying_amount_summary`; emit only the carrying-amount total as an `inventories` ending candidate and keep allowance roll-forward logic separate. |
| Loss allowance financial asset rows | Receivable allowance movement rows may say `기초금융자산`/`기말금융자산` while the allowance measure is only visible in a stacked header row. | Require `손상차손누계액` in the table header stack before extracting loss-allowance candidates; route matched/unexplained/parse-uncertain signed formulas through `note_layout_formula_check` and preserve effective unit tolerance. |
| Financial fair value summary | Fair-value notes split between simple single-column `공정가치` component totals and hierarchy tables with `수준1`/`수준2`/`수준3` columns plus section labels. | Keep simple amount-column tables in `financial_instrument_fair_value_summary`; route hierarchy tables to `financial_fair_value_level_summary`, use the second label cell for instrument names, and verify each row's level components against `합 계` independently. |
| Tax expense composition summary | Income-tax notes mix true tax-expense composition tables with effective-tax-rate reconciliation tables and OCI tax-effect detail schedules under similar `법인세` titles. | Route only simple period-column composition tables to `tax_expense_composition_summary`; require current-tax and deferred-tax movement evidence, include source-backed current tax adjustments/capital-direct tax effects/simple `기타` rows, and exclude `법인세비용차감전`/`적용세율`/`유효세율` and OCI detail rows such as `순확정급여`, `기타포괄손익`, `위험회피`, or `회계정책변경`. |
| Credit risk exposure row summary | Credit-risk maximum-exposure disclosures can be vertical amount-column tables or horizontal row-summary tables, and horizontal components may include non-balance labels such as guarantees or unused commitments. | Keep both shapes in `credit_risk_exposure_summary`; for horizontal rows, sum every source-backed numeric component column before `금융상품 합계` rather than only mapped balance-account columns, and use a generic component key when label mapping is uncertain. |
| Provision current/noncurrent summary | Provision notes can contain current/noncurrent composition summaries that look like roll-forward tables only because the title says `충당부채`. | Keep `provision_current_noncurrent_summary` separate from signed provision roll-forwards; sum component rows per source-backed current/noncurrent column, include provision-account labels that omit literal `충당부채`, and leave movement-column semantics to `provision_rollforward`/`provision_row_rollforward`. |
| Financial category formula summary | Financial-instrument category tables can have plain total headers, duplicated section labels, two label columns, and component columns such as `기타금융자산` that are categories rather than account rows. | Emit formulas only per source-backed account row, use the second label cell when the first cell is a broad section label, skip section header rows and zero components, and leave non-closing row sums as `unexplained_gap` diagnostics rather than forcing matches. |
| Receivable present value carrying summary | Receivable carrying-amount tables can mix gross amount, present-value discounts, loss allowances, deferred loan fee income/cost, and two label columns with broad current/noncurrent buckets. | Keep this layout separate from plain receivable carrying summaries; emit source-backed row components and carrying totals, use the second label cell for specific account keys, and leave rows with zero disclosed adjustments but nonzero carrying differences as `unexplained_gap` rather than silently treating them as rounded matches. |
| Receivable loss-allowance carrying summary | Receivable carrying-amount tables may omit present-value discount columns and instead disclose `총장부금액`, `차감: 손실충당금`/`손실충당금`, and `장부금액 합계`. | Keep the plain carrying layout as source-backed gross-plus-allowance arithmetic while preserving ending candidates for balance reconciliation; promote only matched row formulas into official note layout checks so ambiguous subtotal/account rows stay diagnostic rather than false report gaps. |
| Receivable two-label carrying summary | Receivable carrying tables can put current/noncurrent buckets in the first label column and actual account names in the second label column, causing inventory row labels to look like generic `유동` rows. | Classify these only when receivable titles and explicit gross/allowance/carrying columns are present; combine first-label current/noncurrent context with second-label account names for account keys, skip broad bucket rows whose second label is not a specific receivable account, and keep matched formulas source-backed by row/column coordinates. |
| Receivable loss-allowance aging summary | Receivable loss-allowance detail may be transposed with aging buckets as columns and measures such as `총 장부금액`, `손실충당금`, and `기대 손실률` as rows. | Treat only amount rows (`총 장부금액`, `손실충당금`/`대손충당금`) as bucket-sum formulas, exclude expected-loss-rate rows, and promote only matched row totals into official note layout checks. |
| Inventory carrying formula summary | Inventory carrying tables vary between `총장부금액`, `취득원가`, and `평가전금액` gross headers, and allowance headers range from valuation allowance to impairment accumulated amounts. | Keep one inventory carrying layout but extract row-level source-backed components only when explicit component columns exist; use the second label cell for actual inventory classes in broad-bucket tables, preserve ending-only behavior for one-column inventory summaries, and leave non-closing rows as `unexplained_gap` diagnostics. |
| Lease liability current/non-current summary | Lease notes can show current/non-current liability totals as a simple amount table, while maturity analyses can also contain `유동 리스부채`, `비유동 리스부채`, and `총 리스부채` rows under maturity bucket headers. | Route simple amount-column split tables to `lease_liability_current_noncurrent_summary`; if maturity bucket headers such as `1년 이내`/`1년 초과` are present, keep the table in lease maturity analysis instead of aggregating current/noncurrent rows. |
| Functional expense research allocation | R&D functional allocation tables are internal table arithmetic, not the same primary depreciation/amortization expense allocation check. | Keep research allocation in its own layout and promote only component-to-total formulas; do not feed it into the depreciation/amortization primary reconciliation surface. |
| Employee benefit expense allocation | Retirement/employee-benefit notes may disclose recognized expense allocation by function as component rows and period columns, with labels varying between `판관비`, `판매비`, `관리비`, `매출원가`, and `제조원가`. | Keep this separate from defined-benefit roll-forward and maturity checks; group source-backed components by period column, compare only against explicit `합계`/`총계` rows, and promote only matched formulas into official note layout checks. |
| Employee benefit maturity summary | Defined-benefit expected-payment maturity disclosures share maturity bucket headers with financial-liability liquidity tables but use pension cash-flow semantics and sometimes `1~2년 미만` / `5년 이상` bucket labels. | Keep them in `employee_benefit_maturity_summary`, extract only expected benefit payment rows to `defined_benefit_expected_payments`, and apply display-unit tolerance only after all maturity bucket columns are source-backed. |
| Employee benefit contribution maturity summary | Expected contribution rows can share the same employee-benefit maturity headers as expected-payment rows, but some companies disclose only a total contribution amount without source-backed bucket components. | Map contribution rows to `defined_benefit_expected_contributions` only inside employee-benefit maturity summaries, and create official formulas only when at least two maturity component buckets plus a total are present; total-only rows stay unverified rather than becoming false `actual=0` gaps. |
| Lease liability maturity summary | Lease notes can mix lessee lease-liability maturity rows with lessor receivable lease schedules and current/noncurrent total-only rows in the same maturity-shaped disclosure. | Keep lessee liability rows in `lease_liability_maturity_summary`, exclude `받게 될`/lease receivable/net investment rows, and create formulas only for rows with at least two source-backed maturity bucket components plus a total. |
| Selling/admin expense summary layout | Generic one-column amount tables can be disclosure text or subtotal schedules rather than expense summaries. | Require a selling/admin expense title, `금액` column, recognizable expense rows, and total row; use `expense_summary_total` only as table-level arithmetic diagnostics, not as a replacement for existing expense-allocation primary checks. |
| Net debt bridge layout | Financing-liability cash-flow bridge tables contain multiple account columns, so a single table-level roll-forward can create parse-uncertain or false aggregate formulas. | Classify the bridge as `net_debt_bridge`, preserve source-backed candidates per liability column, and group formulas by `account_key`; future cash-flow reconciliation should consume these grouped candidates rather than re-parsing the same note table. |
| Financing debt bridge generalization | Cash-flow note titles may be generic `현금흐름표` while the table rows use `재무활동에서 생기는 기초/기말 부채` instead of `기초/기말 순부채`. | Require financial-liability account columns plus beginning/ending financing-debt rows and at least one financing-debt movement row; keep duplicate account columns such as current/noncurrent lease liabilities parse-uncertain until a column-disambiguation rule is tested. |
| Lease liability column disambiguation | Financing debt bridge tables may contain generic, current, and noncurrent lease liability columns in the same table. | Preserve each lease column as a separate account key when the header explicitly says `유동성` or `비유동`; do not aggregate current/noncurrent lease movements unless a later reconciliation rule explicitly asks for an aggregate lease-liability cash-flow view. |
| Bond column disambiguation | Financing debt bridge tables may contain short-term bonds, current long-term bonds, current/noncurrent bonds, and convertible/exchangeable bonds in the same table. | Preserve explicit bond subtype headers as separate account keys for formula diagnostics; only aggregate these subtypes later in cash-flow reconciliation when a tested target explicitly requests total bond movement. |
| Rental deposit column disambiguation | Financing debt bridge tables may contain current and noncurrent rental deposit columns in the same table. | Preserve explicit current/noncurrent rental deposit headers as separate account keys for formula diagnostics; do not aggregate them unless a later reconciliation target explicitly requests total rental-deposit movement. |
| Net debt aggregate movement skip | Some DART tables include both an aggregate movement row and detailed movement rows under the same primary label. | In two-label-column bridge tables, skip only signed movement rows where primary and secondary labels are identical and more specific secondary rows follow; keep total-only movement rows when no detail rows exist. |
| Display unit tolerance | DART note tables often disclose rounded amounts in `천원` or `백만원`, so exact arithmetic can differ by one displayed unit even when the disclosed formula is internally complete. | Use candidate `unit_multiplier` as the audit tolerance floor for roll-forward diagnostics; differences above one displayed unit stay as `unexplained_gap` and must not be silently matched. |
| Credit risk exposure summary layout | Financial-risk notes may contain ECL bucket or collateral mitigation tables whose headers include the phrase `신용위험` but are not simple exposure summaries. | Require standalone `신용위험` amount headers plus `익스포저` context for `credit_risk_exposure_summary`; keep ECL/collateral tables in the unknown backlog until a separate multi-column formula is defined. |
| Liquidity maturity analysis layout | Maturity tables often repeat account labels or use the same account key for multiple rows, so grouping by account key alone can create parse-uncertain formulas or hide row-level gaps. | Group `liquidity_maturity_total` formulas by table source, row index, and account key; treat non-closing rows as `unexplained_gap` diagnostics rather than matched evidence. |
| Lease expense summary layout | Lease expense tables mix component rows with total-only rows, and their usable asset class labels may be on the third header row rather than the first. | Use the deepest lease asset header row for component labels, preserve total-only lease expense candidates, and create `lease_expense_total` formulas only for rows with explicit components. |
| Disclosure noise relevance trim | Over-broad asset-word matching can crowd the backlog with policy, related-party, or restriction disclosures. | Add explicit non-validation evidence for disclosure/policy surfaces while preserving the tables in unknown inventory; do not silently drop them from coverage. |
| Unknown layout relevance taxonomy | The unknown backlog can become noisy if all general note tables are treated as validation failures or if CLI diagnostics use a different relevance rule than corpus artifacts. | Keep `validation_relevance` explicit and route both corpus and one-company diagnostics through `classify_validation_relevance()`; use validation-relevant buckets to prioritize formula/layout work while still preserving all unknown tables for audit coverage. |
| Corpus artifact retention | Repeated 100-company corpus runs can duplicate raw DART HTML and generated report HTML into many `out/corpus/run_*` directories, exhausting local disk without adding new verification evidence. | Preserve manifests and summary/JSON taxonomy artifacts; use `dart-footing workpaper-corpus --results-only` for routine goal-mode runs, and keep raw/report artifacts only when actively debugging parser or rendered-report behavior. |
| PPE/ROU acquisition | Plain PPE acquisition and combined PPE+ROU CFS acquisition intentionally differ. | Only include ROU acquisition when `_is_combined_ppe_rou_acquisition_cfs_line()` is true. |
| Baseline metrics | 575-check historical artifacts can be mistaken for current acceptance data. | Use 243-check reproducible local baseline for current acceptance unless cash-flow primary extraction is restored and revalidated. |
| UI/report work | Prior direct UI implementation created process risk. | Future UI changes should be planned and reviewed separately; keep report HTML changes test-backed. |
| Cross-domain work | Non-DART domains can leak into this repo. | Treat `c8ed18d` as the boundary: election workpaper belongs elsewhere. |

## Integration Rules For Future Agents

1. Start by assigning a feature key before editing.
2. Search for the feature key's behavioral surface:
   `rg "<check_id>|<movement_role>|<cli command>|<field name>" src tests docs`.
3. If equivalent behavior already exists, add tests or refine the canonical
   implementation instead of creating a parallel module or role.
4. Keep commits scoped to one feature key. If a file change serves two feature
   keys, split the patch or commit the parts separately.
5. Do not revive reverted cross-domain work in this repository.
6. Before committing, check `git status --short --branch`, `find .git -maxdepth 2
   -name '*.lock' -print`, and `git worktree list --porcelain`.
7. For docs-only diagnostics, keep code untouched and make a docs-only commit.

## Cleanup Log

- 2026-06-03: Pruned stale worktree registration for
  `/private/tmp/dfr-head-compare`.
- 2026-06-03: Removed stale zero-byte `.git/objects/maintenance.lock` after
  checking for active Git maintenance/gc processes.

## Open Coordination Items

- Consider adding a lightweight `docs/validation/current-feature-ledger.md`
  pointer if this ledger becomes the ongoing coordination artifact.
